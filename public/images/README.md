# Save the Bye-Bye Cat GIF Here

## Instructions:

1. **Save the cute cat gif** (the one from your attachment) as: `bye-cat.gif`
2. **Put it in this folder**: `public/images/bye-cat.gif`
3. The gif will automatically appear in the cooldown screen when users chat for too long

## Location:

```
mindora-28h/
  └── public/
      └── images/
          └── bye-cat.gif  ← Save here!
```

The gif is already referenced in the code at:

- `src/components/mindful-chat.tsx` (line ~657)
- Shows when users reach their chat time limit
